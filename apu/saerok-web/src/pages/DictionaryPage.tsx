const DictionaryPage = () => {
  const DESIGN_HEIGHT = 925;
  const scale = window.innerHeight / DESIGN_HEIGHT;

  return <div className="w-full bg-mainBlue h-955"></div>;
};

export default DictionaryPage;
